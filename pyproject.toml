[project]
name = "ru-certs"
version = "0.1.0"
description = "Сборщик доменов Russian Trusted CA из российских CT-логов"
requires-python = ">=3.12"
dependencies = [
    "requests==2.34.2",
    "cryptography==50.0.0",
    "tldextract==5.3.2",
]

[tool.uv]
package = false